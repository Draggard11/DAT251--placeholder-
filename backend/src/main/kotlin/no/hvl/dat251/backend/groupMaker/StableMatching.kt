package no.hvl.dat251.backend.groupMaker

class StableMatching(
    val adjList: HashMap<FavoringUser, Int>?,
    val matching: HashMap<FavoringUser, Int> = HashMap(),
) {
    fun calculate() {
    }
}
